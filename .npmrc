# pnpm ≥ 9 默认 auto-install-peers=true，会去装本包声明的 @deepseek-ai/* peer ——
# 哪怕它们全被标成 optional。而 @deepseek-ai/dsh-client-runtime 的 latest
# (0.0.1-rc.1) 依赖 @deepseek-ai/dsh-compact，后者在 npm 上根本没发布（官方源与
# 镜像源都是 404），于是 `pnpm install` 直接以 ERR_PNPM_FETCH_404 失败。
#
# 那些 peer 是给类型和运行时环境做声明用的，构建一个都不需要（唯一的值导入
# @deepseek-ai/schemastery 在构建时被 external 掉，运行时由 harness 提供）。
# 所以这里关掉自动安装：README 里的 `pnpm install && pnpm build` 才走得通。
auto-install-peers=false
